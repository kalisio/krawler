seeds:
  seed:
    caches: [<%= seed.layer %>_cache]
    coverages: [cell]
    levels:
      to: <%= seed.levelMin %>

coverages:
  cell:
    bbox: [<%= seed.bbox[0] %>, <%= seed.bbox[1] %>, <%= seed.bbox[2] %>, <%= seed.bbox[3] %>]
    srs: 'EPSG:4326'
